/**
 * Persistence helpers that resolve JPA entity references without loading full graphs.
 */
@NullMarked
package com.example.emotion_diary_server.persistence;

import org.jspecify.annotations.NullMarked;
